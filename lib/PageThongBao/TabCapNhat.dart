import 'package:flutter/material.dart';

class CapNhat extends StatelessWidget {
  const CapNhat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Cap Nhat'
        ),
      )
    );
  }
}
