import 'package:flutter/material.dart';

class CloudWORK extends StatefulWidget {
  const CloudWORK({super.key});

  @override
  State<CloudWORK> createState() => _CloudWORKState();
}

class _CloudWORKState extends State<CloudWORK> {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.lightBlue,
    );
  }
}
